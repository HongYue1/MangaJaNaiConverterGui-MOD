# JaNai Upscaler - what the worker needs, installed into backend\python by
# setup.cmd with uv. Mirrors MangaJaNaiConverterGui\backend\src\pyproject.toml
# so the upscaling results stay identical; sanic is here only because the
# backend modules import sanic.log for their logger.
#
# torch and torchvision are resolved against the index uv picks for the GPU it
# detects (setup passes --torch-backend); everything else comes from PyPI.
#
# PySide6-Essentials is the only thing here the interface itself needs. It is
# the trimmed Qt wheel (~77 MB): the full PySide6 also ships WebEngine, 3D,
# Charts and Multimedia, which this app never imports.

PySide6-Essentials==6.11.2

torch==2.9.1
torchvision==0.24.1

spandrel==0.4.1
spandrel_extra_arches==0.2.0
chainner_ext==0.3.10

numpy==2.2.5
opencv-python==4.11.0.86
# pyvips is the Python binding and is safe to move: the same page encoded
# through encode_vips() comes out byte-identical on 3.2.0. pyvips-binary is
# libvips itself and is held at 8.16.1 on purpose - 8.18.6 re-encodes
# (different PNG bytes, and AVIF changed size too: 736 -> 762 B gray,
# 1218 -> 1091 B rgb) and it gains nothing we use, since it still has no
# jxlsave and .jxl still goes out through the bundled cjxl.exe.
pyvips==3.2.0
pyvips-binary==8.16.1
pillow==11.3.0

psutil==7.2.2
pynvml==13.0.1
packaging==26.3
rarfile==4.5
sanic==25.12.1
